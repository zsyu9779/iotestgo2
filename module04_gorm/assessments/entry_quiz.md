# Entry Quiz

1. `gorm.Open` 返回的对象与 `database/sql.DB` 分别负责什么？
2. Java JPA 的 Entity 与 Go 结构体 Tag 有什么差异？
3. 什么是事务的原子性？
4. 为什么 SQL 参数不能直接拼接到字符串？
5. `First` 与 `Find` 在未找到数据时的行为可能有什么差异？

本 Quiz 不计分；第 1、3、4 题答错时，讲师应放慢相应课程 Demo。
