# 文件扫描器故障排查

- 编译失败：先检查是否从独立作业根目录运行，以及 `go env GOMOD` 是否指向本目录。
- 测试超时：检查生产者是否关闭 jobs、协调者是否等待 worker、取消后是否仍在阻塞发送。
- race 报告：不要让多个 worker 直接修改同一个 `Result`；通过结果 Channel 汇总。
- 文件数不稳定：只统计成功扫描的普通文件，不统计目录和符号链接。
- 长行读取失败：为 `bufio.Scanner` 设置明确且有限的最大 token 大小，并传播错误。
