# Module 02 Entry Quiz

1. Go 接口实现是否需要 `implements`？
2. `var p *T = nil; var x I = p` 时，`x == nil` 是否成立？
3. 无缓冲 channel 的发送什么时候完成？
4. `WaitGroup.Add` 应该在启动 goroutine 前还是后调用？
5. Context 的取消信号应该如何传递到下游函数？
6. `go test -race` 能发现哪一类问题？

本测验用于调整课堂节奏，不计入最终成绩。
